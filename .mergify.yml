queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - "label = zzz-never-merge-blocker"
    allow_inplace_checks: false
    batch_size: 1
# autoqueue rule removed to stop unattended speculative re-checks; re-add a `queue` action to resume.
pull_request_rules: []
