queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - "label = zzz-never-merge-blocker"
    allow_inplace_checks: false
    batch_size: 1
pull_request_rules:
  - name: autoqueue-test
    conditions:
      - base = main
    actions:
      queue:
        name: default
